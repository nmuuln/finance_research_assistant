# GCP / Discovery Engine (Deep Research) config
PROJECT_ID=your-gcp-project-id
GDR_APP_ID=your-gdr-app-id
GDR_DATA_STORE_ID=        # optional; leave blank if not using a Vertex AI Search datastore
GDR_LOCATION=       # usually "global"
GDR_COLLECTION=your-gdr-collection-id
GDR_ASSISTANT=your-gdr-assistant-id

# Gemini API (for WriterAgent). If using AI Studio:
GOOGLE_API_KEY=your-google-api-key

# Tavily API (for web search)
TAVILY_API_KEY=your-tavily-api-key

# Default topic (you can override via CLI arg)
TOPIC=Credit risk transmission from monetary tightening to SME default rates (2019–2025, emerging markets)

# ====================================================================
# DigitalOcean Spaces Configuration (for cloud file storage)
# ====================================================================
# Get your credentials from: https://cloud.digitalocean.com/account/api/spaces
# 1. Click "Generate New Key"
# 2. Copy the Access Key (starts with DO00...)
# 3. Copy the Secret Key (long random string - only shown once!)
# 4. Replace the placeholders below with your actual keys

SPACES_ACCESS_KEY=YOUR_DO_SPACES_ACCESS_KEY_HERE
SPACES_SECRET_KEY=YOUR_DO_SPACES_SECRET_KEY_HERE
SPACES_REGION=sgp1
SPACES_BUCKET=finance-bucket

# Note: After adding real keys, restart the server for changes to take effect
